---
stages:
  - lint
lint:
  image: python:3.12
  script:
    - pip install ansible ansible-lint yamllint
    - ansible-playbook --syntax-check playbooks/harden.yml -i inventories/production/hosts.yml
