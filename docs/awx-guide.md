# AWX Guide

- Create a project from this repository.
- Use `inventories/awx/hosts.yml` or a custom inventory.
- Use `playbooks/harden.yml` as the job template playbook.
- Provide credentials for SSH or local execution.
- Apply survey variables from `docs/awx-survey-examples.yml`.
